{
    /* B站海外CDN域名（除非你知道这是什么，否则请不要修改） */
    host: 'upos-hz-mirrorakam.akamaized.net',
    /* 代理服务器运行的端口 */
    port: 7070,
    /* 重新查找最优节点的间隔时间(s) */
    refreshInterval: 3600,
    refreshIpList: {
        /* 重新查找新增节点的间隔时间(s) */
        interval: 86400,
        /* 有关chinaz服务器节点的重试配置（除非你知道这是什么，否则请不要修改） */
        retry: {
            times: 50,
            interval: 10000
        }
    },
    /* 是否保存新增的节点 */
    saveChinazResult: true,
}